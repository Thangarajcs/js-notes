Handling Errors with try-catch:

try {
    //normal codr
}
catch(err){
// from try block error will be caught here
}
finally{

}



function sum(a,b) {

    try {
      let x = 9;
      x = x + 2;
  
    } catch (eeeee) {
      console.log('my error',eeeee)
      // let para = document.createElement('p');
      // para.innerText = 'please check your inputs dear user'
    
      // document.getElementById('img-container').appendChild(para);
    }finally{
      console.log('i m done')
    }
  }
  
  sum(0,0)


  API:APPLICATION PROGRAMMING INTERFACE

fetch():

allows u to make HTTP req to ServiceWorkerRegistration
no need to use XMLHttpRequest

  fetch() api - browser api



  //Task for friday:

  url: https://dummyjson.com/products
  method: GET

  get the responseText 
  then convert to json parse

  then access the array obj - res.products

  //ui part 
  u can show brand or img or brand + price


  
